# Change Log

| Date | Commit | File | Section | Lines | Change Type | Summary | Reason | Status |
|------|--------|------|---------|-------|-------------|---------|--------|--------|
| 2026-05-09 | `0cd854d` | `sections/header.liquid` | JSON-LD Organization `sameAs` | 336-346 | Modified | Skip blank `social_*_link` values; add LinkedIn and WhatsApp | Empty strings in `sameAs` flagged by SEO crawlers | Pushed to `main` |
| 2026-05-11 | `e0c23a2` | `sections/featured-home-collection.liquid` | Homepage product card loop | 76-114 | Added | Emit per-product schema.org `Product` JSON-LD (name / image / price / availability / brand / sku / url) with `@id` for dedup | Homepage product blocks were invisible to Google rich results and AI shopping agents; structured data unlocks rich product cards and lets assistants quote prices | Pushed to `main` |
| 2026-05-11 | `7f0bbc1` | `sections/Breadcrumb.json` | New section group | 1 | Added | New section-group JSON that wraps the existing Breadcrumb section so the dormant `{% sections 'Breadcrumb' %}` call in `theme.liquid` finally renders sitewide | `theme.liquid:194` already called the section group but the JSON wrapper was missing — breadcrumbs only appeared on 4 templates that included them inline | Pushed to `main` |
| 2026-05-11 | `7f0bbc1` | `sections/Breadcrumb.liquid` | Inside breadcrumb container | 381-411 | Added | Append schema.org `BreadcrumbList` JSON-LD mirroring the visible Home → depth1 → depth2 → depth3 → current-page hierarchy | SEO: structured data lets Google show breadcrumb rich results and helps crawlers/AI agents understand site hierarchy | Pushed to `main` |
| 2026-05-11 | `7f0bbc1` | `templates/{collection.json, collection.collection-new-design.json, collection.new-temp-2026.json, product.json}` | Inline breadcrumb section entries | various | Removed | Strip inline `Breadcrumb` section + corresponding `order` entry from 4 templates that previously rendered breadcrumbs inline | Avoid double-rendering once the sitewide section group renders breadcrumbs above main content | Pushed to `main` |
| 2026-05-11 | `a816cc2` | `layout/theme.liquid` | Marketing/analytics deferred loader | 179-188 | Modified | Defer `loadAll()` out of the click/touchstart/keydown task via `requestIdleCallback` / `setTimeout` so the interaction's paint completes before analytics script tags are created | INP optimization: previously the first user interaction's reported latency included the analytics-loader work; deferring it removes that contribution | Pushed to `main` |
| 2026-05-11 | `a816cc2` | `layout/theme.liquid` | Homepage `<style>` block | 51-58 | Added | Add `content-visibility: auto` + `contain-intrinsic-size: 0 800px` to below-the-fold homepage sections (featured-home-collection / social-media-icons / dropshipping-section) | INP/render perf: browser skips layout & paint for offscreen sections until they near the viewport | Pushed to `main` |
| 2026-05-11 | `d449273` | `layout/theme.liquid` | Work Sans Google Fonts URL | 105 / 107 | Modified | Change `&display=swap` to `&display=optional` so the browser keeps the system fallback font instead of swapping when Work Sans arrives | **CLS fix**: the swap caused a visible text reflow on every first load. With `optional` the swap is skipped on slow connections | Pushed to `main` |
| 2026-05-11 | `d449273` | `sections/utility-bar.liquid` | Google Play / App Store images | 17-33 | Modified | Add explicit `width=135 height=40` and `decoding=async`; remove `loading=lazy` since these are above the fold | **CLS fix**: lazy-loaded above-fold images with no dimensions caused the whole page below the utility bar to push down on load | Pushed to `main` |
| 2026-05-11 | `d449273` | `sections/slideshow.liquid` | LCP preload for mobile slideshow image | 14-30 | Modified | Change preload `href` from 750w → 550w (matches typical mobile DPR-adjusted viewport); drop the unused 1500w srcset entry | **LCP fix**: mobile devices were preloading a larger image than they need | Pushed to `main` |
| 2026-05-11 | `9b10711` | `snippets/card-product-coll.liquid` | Quick-view + verified-brand badge `<img>` tags | 30 / 68 | Modified | Add explicit `width`/`height` and empty `alt=""` to the `view.png` and `verify.png` icons in product card | **CLS fix**: 40 unsized lazy images was the single biggest CLS contributor on the homepage | Pushed to `main` |
| 2026-05-11 | `9b10711` | `snippets/header-drawer.liquid` | Mobile "just arrived" product thumbnails | 391 | Modified | Replace deprecated `img_url: '200x'` with `image_url: width: 200`; add `width=200 height=(computed from aspect_ratio)` and escape alt text | **CLS fix**: 6 unsized thumbnails in mobile menu drawer | Pushed to `main` |
| 2026-05-11 | `9b10711` | `layout/theme.liquid` | Cart drawer / totals / discounts CSS block | 109-122 | Modified | Move cart-drawer + component-cart + component-totals + component-discounts to async loading; keep component-price render-blocking (above-fold); add noscript fallback | **FCP/LCP fix**: render-blocking CSS for hidden cart UI was wasted work | Pushed to `main` |
| 2026-05-11 | `9b10711` | `sections/*.liquid, snippets/*.liquid` (24 files, 38 call sites) | Section stylesheet includes (duplicated CSS) | various | Modified | Convert `\| stylesheet_tag` calls to async pattern for the most-duplicated stylesheets: `component-card`, `component-slider`, `section-collection-list`, `quantity-popover`, `custom-collection-list` | **FCP/LCP fix**: same CSS files were render-blocking 3-6× via separate section includes | Pushed to `main` |
| 2026-05-11 | `368c4ce` | `layout/theme.liquid` | Homepage `<style>` — content-visibility + slideshow placeholder | 51-63 | Modified | Fix broken `content-visibility` selector (switch from `[id^=…]` to `[id*=…]` since Shopify IDs have a `template--{numeric}__` prefix), reduce `contain-intrinsic-size` from 800px to 500px, and add brand-red background to slideshow image wrapper | **Bugfix + visual**: previous selector matched nothing (silently dead). Brand-color placeholder reduces the perceived 'blank flash' while the LCP banner downloads | Pushed to `main` |
| 2026-05-11 | `b443bca` | `layout/theme.liquid` | Critical inline CSS before async stylesheets | 49-52 | Added | Inline `cart-drawer { position: fixed; top: 0; left: 0 }` so the cart-drawer element is removed from normal flow even before its async CSS arrives | **Bugfix**: the white/blank area at the top of the page during initial load was the cart-drawer element taking ~400px of layout space due to default block flow before its async CSS applied `position:fixed` | Pushed to `main` |
| 2026-05-11 | `05545b8` | `layout/theme.liquid` | Homepage `<style>` — content-visibility + slideshow placeholder | 53-67 | Removed | Revert the content-visibility + red-slideshow-placeholder CSS from commit `368c4ce`. The `[id*="featured_home_collection"]` selector also matched each product card's `<li>` (their IDs contain that string), applying `content-visibility:auto` to every card and breaking flex layout on mobile. The red placeholder was overflowing during load. | **Visual fix**: undo the aggressive optimizations that were breaking mobile card layout and showing an unwanted full-viewport red flash. All other beneficial changes preserved. | Pushed to `main` |
| 2026-05-11 | _pending_ | `layout/theme.liquid` | Hreflang tags (sitewide `<head>`) | 49-50 | Added | Add `<link rel="alternate" hreflang="en-IN" href="{{ canonical_url }}">` and `<link rel="alternate" hreflang="x-default" href="{{ canonical_url }}">` after the canonical block | **SEO**: explicitly declares to Google that the site serves the Indian market in English. The `x-default` is the recommended fallback per Google's hreflang spec | Pushed to `main` |

